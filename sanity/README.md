# Balthazaurus Gatsby & Sanity Starter

run "npm start" to develop
run "sanity graphql deploy production" to deploy any schema changes to the GraphQL API. Don't forget to also restart Gatsby after doing this.